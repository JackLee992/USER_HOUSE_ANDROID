Use case: precise-object-edit.
Input image is the edit target: this nine-game icon atlas.
Correct only the atlas layout, tile framing and block count. Preserve the existing nine subjects, order, rich tactile 3D style, warm lighting, navy-and-cream-gold palette and all other artwork.
The output must be a geometrically exact 3 by 3 grid of NINE EQUALLY SIZED SQUARE ICONS. NO stretched or taller bottom row. The cell boundaries must fall EXACTLY at x=one-third, x=two-thirds, y=one-third, y=two-thirds of the square canvas. For a 1254px canvas, each cell is exactly 418x418 and boundaries at 418 and 836. Design the same invariant proportion if using another square canvas size.
Each square cell must contain one COMPLETE icon frame and subject wholly inside that square cell, with matching 3% internal padding on all four sides. There must never be part of the next frame entering the previous cell. Each cell is an independent centered composition and uses the same thin cream-and-champagne-gold rounded-square frame. Do not draw one big frame around the whole atlas.
Make the outside corners of each rounded icon solid deep midnight navy, not transparent, not white, not noisy. Full image opaque. No margins around the complete atlas. Each row occupies exactly one third of the canvas height. It is acceptable to scale and center each existing composition to fit its exact square cell.
Keep top row Tetris, snake, 2048; middle row watermelon, matching strawberry cards, jumping fox; bottom row bridge-walking fox, Sudoku, Minesweeper. In Tetris make the falling cyan I piece exactly FOUR connected square blocks, not three. Keep "2048" exactly and Sudoku numbers 1, 2, 3 exactly. Nothing else to add. No labels, no badges, no new text, no watermark. This is a production sprite sheet sampled by exact thirds in CSS, so perfect grid registration is mandatory.
